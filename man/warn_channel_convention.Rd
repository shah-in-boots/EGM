% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wfdb-annotation.R
\name{warn_channel_convention}
\alias{warn_channel_convention}
\title{Report a channel column that looks signal-numbered while read as global}
\usage{
warn_channel_convention(channel, header, channel_zero, record, annotator)
}
\arguments{
\item{channel}{The channel column as read.}

\item{header}{The record's \code{header_table}, or \code{NULL}.}

\item{channel_zero}{The convention the caller declared.}

\item{record, annotator}{Named in the message.}
}
\value{
Nothing, called for the warning.
}
\description{
The one place the two conventions can be told apart, because it
is the only one holding both the channels and the number of signals the
header declares. Channels running \verb{0 .. nsig-1} exactly fill the signals, so
the \code{0} in them is the first lead and not an absence of information.
}
\details{
Read as global, such a file loses its first lead: those annotations
are treated as belonging to no lead, so they are retained alongside every
other channel rather than being selectable as one. Nothing downstream can
notice, which is why it is said here, once, where the file is opened.
}
\keyword{internal}
