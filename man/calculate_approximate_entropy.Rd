% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fwave.R
\name{calculate_approximate_entropy}
\alias{calculate_approximate_entropy}
\title{Calculate Approximate Entropy (ApEn) of a time series}
\usage{
calculate_approximate_entropy(x, m = 2, r = NULL)
}
\arguments{
\item{x}{Numeric vector of the time series}

\item{m}{Embedding dimension. Default 2.}

\item{r}{Tolerance. Default \code{NULL}, which uses 0.2 times the standard
deviation of \code{x}.}
}
\value{
Approximate entropy value
}
\description{
Computes approximate entropy by the method of Pincus (1991),
comparing vectors embedded in \code{m} and \code{m + 1} dimensions and taking the
difference of the resulting correlation integrals.
}
\details{
Approximate entropy counts self-matches, which biases it toward
regularity and makes it dependent on record length.
\code{\link[=calculate_sample_entropy]{calculate_sample_entropy()}} avoids both and is generally preferable.

The tolerance \code{r} defaults to 0.2 times the standard deviation of \code{x}. Note
that this default changed: earlier versions used 3.5 times the standard
deviation, which admitted nearly every pair of vectors as a match and drove
the statistic toward zero regardless of the input.

The calculation is O(n^2). Decimate before calling; see
\code{\link[=calculate_sample_entropy]{calculate_sample_entropy()}} for how far.
}
\examples{
set.seed(123)
calculate_approximate_entropy(rnorm(500))

}
\references{
Pincus SM. Approximate entropy as a measure of system complexity.
\emph{Proceedings of the National Academy of Sciences of the USA}.
1991;88(6):2297-2301. \doi{10.1073/pnas.88.6.2297}
}
