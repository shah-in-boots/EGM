% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectorcardiogram.R
\name{assemble_loops}
\alias{assemble_loops}
\title{Assemble traced beats into the tables the caller gets back}
\usage{
assemble_loops(traced, wave, repolarization = NULL)
}
\arguments{
\item{traced}{The result of \code{\link[=trace_loops]{trace_loops()}}.}

\item{wave}{The wave whose loop is returned, e.g. \code{"QRS"}.}

\item{repolarization}{Optional second wave to measure the first against.}
}
\value{
A \code{list} of \code{loop} and \code{components}, both \code{data.table}s.
}
\description{
Takes what \code{\link[=trace_loops]{trace_loops()}} cut and returns the pair both
\code{\link[=vcg]{vcg()}} hands back: the named wave's loop, beat by beat, and the components
read off it.
}
\details{
Naming a \code{repolarization} wave adds the global electric heterogeneity
components. Those describe the discordance between depolarization and
repolarization, so they need both loops and the span between them; a beat
without the second wave delineated keeps its loop and takes \code{NA} for them.
}
\keyword{internal}
