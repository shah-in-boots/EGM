% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequency.R
\name{assert_declared_frequency}
\alias{assert_declared_frequency}
\title{Confirm a declared source rate against the recorded one}
\usage{
assert_declared_frequency(actual, from)
}
\arguments{
\item{actual}{Recorded rate(s), as returned by \code{\link[=frequency]{frequency()}}.}

\item{from}{The rate the caller declared.}
}
\value{
\code{from}, invisibly.
}
\description{
\code{\link[=change_frequency]{change_frequency()}} takes the source rate as an argument so that
the caller has to know it. That only helps if the claim is checked, so this
compares it against whatever the object's own header reports.
}
\details{
A record with no usable recorded rate is accepted on the caller's
word - there is nothing to contradict, and the declared rate repairs the
header. A \code{windows} collection that mixes rates cannot be described by a
single \code{from}, so it fails here rather than silently mis-scaling part of the
collection; convert each source record before combining them.
}
\keyword{internal}
