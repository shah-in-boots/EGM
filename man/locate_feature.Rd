% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{locate_feature}
\alias{locate_feature}
\title{Locate a fiducial sample within an annotation table}
\usage{
locate_feature(
  ann,
  feature,
  channel_criteria = NULL,
  multiple = c("first", "error")
)
}
\arguments{
\item{ann}{An \code{annotation_table} (or compatible \code{data.table}).}

\item{feature}{A \code{character} type symbol or a named list of criteria.}

\item{channel_criteria}{Optional guiding channel number.}

\item{multiple}{How to handle multiple matches: return the \code{"first"}
(default), or raise an \code{"error"}.}
}
\value{
An integer sample index, or \code{NA_integer_} when no match is found.
}
\description{
Returns the sample index of the first annotation that matches a
feature specification. This is the common lookup used to anchor padding
(\code{\link[=pad_window]{pad_window()}}), aligned medians (\code{\link[=median_window]{median_window()}}) and landmark
template learning/warping (\code{\link[=learn_template]{learn_template()}}, \code{\link[=warp_window]{warp_window()}}) on a fiducial
such as the QRS peak.
}
\details{
The feature may be given as a bare \code{character} (matched against the
\code{type} column, e.g. \code{"N"} for the QRS peak) or as a named \code{list} of
criteria (matched column-by-column, e.g. \code{list(type = "(", wave = "P")}).
When a \code{wave} criterion is requested the positional wave labels are recovered
on demand via \code{\link[=label_waves]{label_waves()}}, mirroring how \code{\link[=by_rhythm]{by_rhythm()}} resolves
P/QRS/T identity. When \code{channel_criteria} is supplied and the annotations
carry a \code{channel} column, the search is restricted to that channel plus the
global channel \code{0}, unless the feature itself already pins a \code{channel}.
}
\keyword{internal}
