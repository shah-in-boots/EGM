% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/windows-extract.R
\name{get_windows}
\alias{get_windows}
\title{Extract windows of signal from a record}
\usage{
get_windows(object, by = "rhythm")
}
\arguments{
\item{object}{An \code{EGM} object, including header, signal, and annotation
information.}

\item{by}{A \link{window_strategy} object, or the name of one as a string.
Defaults to \code{"rhythm"}.}
}
\value{
A \link{windows} object: a collection of \code{EGM} objects, one per window.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Searches a record for every span of signal that satisfies a windowing
strategy, and returns each span as its own \code{EGM}. This is how a whole study is
broken into the analysable segments - individual beats, mapping windows,
intervals of interest - that the rest of the window functions operate on.
}
\details{
The strategy may be given as a bare name for its defaults, or built
explicitly with a \verb{by_*()} constructor when its arguments need setting:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{get_windows(ecg, by = "rhythm")
get_windows(ecg, by = by_rhythm(channel = 2))
}\if{html}{\out{</div>}}

Both forms route through the same constructor, so both are validated the
same way. Strategy arguments belong to the constructor rather than to this
function, which is what allows a mistyped argument to be reported instead of
silently ignored.

Each returned window is a complete \code{EGM}: its own header (named
\verb{<record>_<strategy><n>}, with an info string recording the source sample
range), the signal for every channel, and the annotations falling inside it.
Windows are generally \emph{not} the same length, since they follow the signal
rather than a fixed duration; see \code{\link[=pad_window]{pad_window()}}, \code{\link[=normalize_window]{normalize_window()}}, and
\code{\link[=warp_window]{warp_window()}} for the ways to bring them onto a common basis.
}
\examples{
\dontrun{
ecg <- read_wfdb("ecg", test_path(), "ecgpuwave")

# Sinus beats, using the strategy defaults
beats <- get_windows(ecg, by = "rhythm")

# Guided by a single lead, then collapsed to a median beat
get_windows(ecg, by = by_rhythm(channel = 2)) |>
  median_window(align_feature = "N", channel_criteria = 2)
}

}
\seealso{
\code{\link[=by_rhythm]{by_rhythm()}} for the available strategy, \code{\link[=pad_window]{pad_window()}},
\code{\link[=normalize_window]{normalize_window()}} and \code{\link[=warp_window]{warp_window()}} to bring windows onto a common
length, and \code{\link[=change_frequency]{change_frequency()}} to harmonise sampling rates either before
windowing or on the returned collection.
}
