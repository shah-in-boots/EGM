% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/windows-transform.R
\name{warp_window}
\alias{warp_window}
\title{Landmark-warp windows onto a template}
\usage{
warp_window(
  x,
  template,
  interpolation_method = c("linear", "spline", "step"),
  channel = NULL,
  preserve_amplitude = FALSE,
  preserve_class = TRUE,
  missing = c("partial", "drop", "error"),
  ambiguous = c("error", "first", "drop"),
  order_policy = c("error", "drop"),
  ...
)
}
\arguments{
\item{x}{A \code{windows} object (or list of \code{EGM} objects) to warp.}

\item{template}{A \link{template} created manually with \code{\link[=template]{template()}} or learned
with \code{\link[=learn_template]{learn_template()}}. Plain lists are not accepted.}

\item{interpolation_method}{Interpolation method for the warp. One of "linear"
(default), "spline", or "step".}

\item{channel}{Fallback guiding channel used when locating a landmark whose
own spec does not name one, given as a channel number or name. A landmark's
own channel always takes precedence; see the channels section.}

\item{preserve_amplitude}{Logical. If TRUE, rescales each lead back to its
original amplitude range after warping. Defaults to FALSE.}

\item{preserve_class}{Logical. If TRUE (default), returns a \code{windows} object;
if FALSE, returns a plain list of \code{EGM} objects.}

\item{missing}{What to do when a required landmark is absent: warp with the
remaining landmarks, drop that window, or error.}

\item{ambiguous}{What to do when a landmark matches multiple annotations:
error, use the first, or drop that window.}

\item{order_policy}{What to do when landmarks are crossed or duplicated:
error or drop that window.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{windows} object (or list) of landmark-warped \code{EGM} objects, each
\code{target_samples} long (as defined by the template).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Warps each window so that its landmarks (located from annotations) align to
the positions held in a \link{template}, typically created by \code{\link[=learn_template]{learn_template()}}.
Between consecutive landmarks the time axis is stretched or compressed
piecewise-linearly, giving a landmark-aligned beat on the template's
common time base. This is the most flexible - and most destructive - of the
standardization methods, aligning multiple fiducials at once rather than a
single anchor.
}
\details{
Before warping, each landmark defined by the template is
confirmed against the windows; landmarks missing from some windows raise a
warning (those windows warp on their remaining anchors) and landmarks absent
from every window are dropped. For every window the landmark samples are
located - each on its own channel, so a multi-channel template is
honoured - and combined with the window's own start/end as edge anchors,
forming a set of \code{(source -> target)} breakpoints. Output samples are mapped
back to source positions by inverting this piecewise-linear correspondence,
and each lead is interpolated there; annotations are carried forward through
the same map. Any breakpoints that are non-increasing in either axis are
collapsed to keep the mapping monotonic. All leads are warped regardless of
which channel a landmark was detected on.
}
\section{Guiding channel}{


A delineating annotator may be run over a record once, or once per lead. Run
per lead it writes twelve independent copies of every fiducial, separated
only by the \code{channel} column, and such a table does not describe beats until
it is resolved to a single lead: pooled, it reports twelve times as many
beats as the record contains, and taking the first match at each point
silently follows whichever lead happens to sort first.

So every function that reads fiducials - \code{\link[=get_windows]{get_windows()}},
\code{\link[=learn_template]{learn_template()}}, \code{\link[=median_window]{median_window()}} and the other window transforms,
\code{\link[=extract_f_waves]{extract_f_waves()}}, \code{\link[=vcg]{vcg()}} - takes a \code{channel}, and resolves
it the same way:
\itemize{
\item annotations spanning more than one channel with no \code{channel} given are an
error, not a guess;
\item a \code{channel} that the annotations do not carry is an error, which is what
catches a numbering convention that does not match;
\item the global channel \code{0} is not a lead, so it never counts toward the span
and is always kept alongside the chosen lead.
}

The channel may be a number or a channel name (\code{"II"}), resolved against the
record's own header. A number is matched literally against the \code{channel}
column, and the numbering there belongs to the annotator that wrote the
file. Read it off the record rather than assuming:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{table(get_annotation(x)$channel)
}\if{html}{\out{</div>}}

Only the boundaries are restricted to one lead. Windows still carry the
signal for every channel, which is what makes a windowed beat usable as a
vectorcardiogram.
}

\seealso{
\code{\link[=learn_template]{learn_template()}}, \code{\link[=template]{template()}}, \code{\link[=landmark]{landmark()}}
}
