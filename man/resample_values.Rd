% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequency.R
\name{resample_values}
\alias{resample_values}
\title{Resample a single lead onto a new sampling grid}
\usage{
resample_values(values, ratio, method, anti_alias)
}
\arguments{
\item{values}{A \code{numeric} lead.}

\item{ratio}{The frequency ratio (target / source).}

\item{method}{One of "linear", "spline", "step", or "polyphase".}

\item{anti_alias}{Whether to low-pass filter before down-sampling. Ignored by
the \code{polyphase} method, which filters internally.}
}
\value{
The resampled lead.
}
\description{
The shared engine behind \code{\link[=change_frequency]{change_frequency()}}. It works purely in
terms of the frequency ratio, so it is indifferent to whether the lead came
from an \code{EGM} or was handed over as a bare vector.
}
\details{
The endpoints stay fixed: \code{(n - 1)} source steps become
\code{(n - 1) * ratio} output steps, and each output index is projected back to a
real source position so the interpolation samples true morphology rather
than a warped copy. The \code{polyphase} method instead delegates to
\code{\link[signal:resample]{signal::resample()}}, whose own output length is then authoritative.
}
\keyword{internal}
