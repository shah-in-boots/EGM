% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channels.R
\name{require_window_channel}
\alias{require_window_channel}
\title{Resolve the guiding channel for a collection of windows}
\usage{
require_window_channel(windows, channel, what)
}
\arguments{
\item{windows}{A list of \code{EGM} objects.}

\item{channel}{The validated channel argument, as returned by
\code{\link[=valid_channel]{valid_channel()}}.}

\item{what}{A \code{character} naming the caller, used to open the error message.}
}
\value{
\code{NULL} when no restriction is needed, otherwise the channel number.
}
\description{
The window transforms locate a fiducial in every window of a
collection, so they apply the channel policy once, against the first window
that carries annotations. The windows of a collection come from one record
and share its annotator, so one is representative of all.
}
\details{
A channel \emph{name} is also resolved to a number here, against the first
window's own header, since the functions downstream match the \code{channel}
column numerically.
}
\keyword{internal}
