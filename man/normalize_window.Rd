% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window.R
\name{normalize_window}
\alias{normalize_window}
\title{Time-normalize whole windows to a fixed length}
\usage{
normalize_window(
  x,
  target_samples = 500L,
  target_ms = NULL,
  interpolation_method = c("linear", "spline", "step"),
  preserve_amplitude = FALSE,
  preserve_class = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{windowed} object (or list of \code{EGM} objects) to normalize.}

\item{target_samples}{Desired output length in samples. Default 500.}

\item{target_ms}{Alternative target length in milliseconds, converted using
the first window's sampling frequency. Used only when \code{target_samples} is
\code{NULL}.}

\item{interpolation_method}{Interpolation method for the stretch. One of
"linear" (default), "spline", or "step".}

\item{preserve_amplitude}{Logical. If TRUE, rescales each lead back to its
original amplitude range after interpolation. Defaults to FALSE, since a
pure time stretch should leave amplitudes to the interpolation.}

\item{preserve_class}{Logical. If TRUE (default), returns a \code{windowed} object;
if FALSE, returns a plain list of \code{EGM} objects.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{windowed} object (or list) of time-normalized \code{EGM} objects, each
exactly \code{target_samples} long.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Stretches or compresses each window as a whole so that every window spans the
same number of samples, mapping the left and right borders (e.g. P-onset and
T-offset for a sinus beat) onto a common \verb{[0, target_samples)} grid. This is a
\emph{destructive} standardization - the time axis is warped, so absolute intervals
are no longer comparable - but it is a fast, simple way to bring ragged beats
onto a shared basis for averaging or matrix operations.
}
\details{
This is the whole-window (border-to-border) counterpart to the
feature-anchored resampling in \code{\link[=standardize_windows]{standardize_windows()}}; internally it reuses
the same interpolation engine with alignment disabled. For alignment on a
fiducial without warping the time axis, use \code{\link[=pad_window]{pad_window()}} instead; to change
the physical sampling rate while preserving duration, use \code{\link[=resample_window]{resample_window()}}.
}
