% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window.R
\name{resample_egm}
\alias{resample_egm}
\title{Resample a single \code{EGM} to a target sampling frequency}
\usage{
resample_egm(object, target_frequency, interpolation_method = "linear")
}
\arguments{
\item{object}{An \code{EGM} window.}

\item{target_frequency}{Target sampling rate in Hz (positive).}

\item{interpolation_method}{One of "linear", "spline", or "step".}
}
\value{
A resampled \code{EGM} object.
}
\description{
Duration-preserving resample of one window. The signal is
re-interpolated onto a new grid whose length scales with the frequency
ratio, the header frequency/sample-count are updated, and annotation sample
indices are rescaled to the new grid. This is the per-window engine behind
\code{\link[=resample_window]{resample_window()}} and the \code{resample_frequency} argument of \code{\link[=window]{window()}}.
}
\details{
A window read at 250 Hz and resampled to 500 Hz keeps the same
wall-clock duration but gains (approximately) twice the samples; morphology
timing is preserved because each output sample maps back to a real source
position (\code{output / ratio}). Frequencies that already match are a no-op.
}
\keyword{internal}
