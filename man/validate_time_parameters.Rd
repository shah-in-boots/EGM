% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wfdb-utils.R
\name{validate_time_parameters}
\alias{validate_time_parameters}
\title{Validate WFDB time parameters}
\usage{
validate_time_parameters(
  begin = NULL,
  end = NULL,
  interval = NULL,
  start_time = as.POSIXct(NA),
  study_duration = NA_real_
)
}
\arguments{
\item{begin, end}{A single time value. Character values may use
\code{"HH:MM:SS"}, \code{"YYYY-MM-DD HH:MM:SS"}, or the WFDB-style
\code{"HH:MM:SS DD/MM/YYYY"} format, with optional fractional seconds and
optional square brackets around a dated WFDB timestamp.
\code{POSIXt}, \code{hms}, \code{ITime}, and \code{difftime} objects are also supported.
A \code{difftime} value is interpreted as elapsed time from the record start.}

\item{interval}{A single duration. Numeric values are seconds. Character
values combine a number and unit, such as \code{"30s"}, \code{"100 ms"}, \code{"2 min"},
or \code{"1.5 hours"}. A \code{difftime} value is also accepted.}

\item{start_time}{The record start as a \code{POSIXt} value. This is required for
absolute \code{begin} and \code{end} values, but not for elapsed time-only values.}

\item{study_duration}{The available record duration in seconds. When known,
requested endpoints are clamped to this duration. Records longer than one
day require dated \code{begin} and \code{end} values instead of time-only clocks.}
}
\value{
A list containing normalized \code{begin}, \code{end}, and \code{interval} values
in elapsed seconds from the beginning of the record.
}
\description{
\code{validate_time_parameters()} validates and normalizes the time window used
by the WFDB readers. Time-only \code{begin} and \code{end} values are elapsed from the
record start, consistent with WFDB standard time. Dated values are absolute.
\code{interval} is a duration measured from \code{begin}.
}
