% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/windows-transform.R
\name{pad_window}
\alias{pad_window}
\title{Pad windows to a common length}
\usage{
pad_window(
  x,
  target_samples = NULL,
  target_ms = NULL,
  align = c("center", "left", "right", "feature"),
  align_feature = "N",
  feature_position = NULL,
  channel = NULL,
  pad_value = NA,
  preserve_class = TRUE,
  channel_criteria = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{windows} object (or list of \code{EGM} objects) to pad.}

\item{target_samples}{Desired output length in samples. If \code{NULL} (default),
the length is derived from the data (the longest window, or the feature-span
for \code{align = "feature"}).}

\item{target_ms}{Alternative target length in milliseconds, converted to
samples using the first window's sampling frequency. Ignored when
\code{target_samples} is supplied.}

\item{align}{One of "center" (default), "left", "right", or "feature". See
details.}

\item{align_feature}{Feature used for \code{align = "feature"}, given as a
\code{character} type symbol (default "N", the QRS peak) or a named list of
annotation criteria. Ignored for other alignments.}

\item{feature_position}{Target index (0-based) for the aligned feature when
\code{align = "feature"} and \code{target_samples} is supplied. Defaults to the window
centre.}

\item{channel}{The lead whose \code{align_feature} anchors the alignment, given as
a channel number or name. Required when the annotations span more than one
channel; see the channels section.}

\item{pad_value}{The value used for padding: \code{NA} (default), a single number,
or the string \code{"edge"}. \code{NA} marks the added samples as absent rather than
as a measurement. Padding with \code{0} states that the potential there was zero,
which is a fabricated observation: it biases \code{\link[=median_window]{median_window()}} toward the
origin at the edges of the beat, and with it every measure read off the loop
tails. \code{"edge"} extends the nearest observed sample outward instead. See the
fill section for choosing between them.}

\item{preserve_class}{Logical. If TRUE (default), returns a \code{windows} object;
if FALSE, returns a plain list of \code{EGM} objects.}

\item{channel_criteria}{Superseded name for \code{channel}, still accepted.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{windows} object (or list) of padded \code{EGM} objects, all sharing the
same sample length. Amplitudes are untouched, so whatever DC offset the
recording carried is still there; \code{\link[=baseline_window]{baseline_window()}} removes it, and
variance-based methods find it first if it is left in.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Extends each window with \code{pad_value} so that every window shares a common
length. Unlike time normalization, padding is \emph{non-destructive}: the original
samples are preserved and only the edges are added to. Use it to anchor a
fiducial - typically the QRS peak - at a fixed index across windows, or to
make ragged windows rectangular for stacking.

Where the windows can be cut equal in the first place, \code{\link[=by_beat]{by_beat()}} is the
better route: padding marks the added samples absent, but absent samples
still thin out the reduction that follows.
}
\details{
The \code{align} argument controls where the real signal sits within the
padded window:
\itemize{
\item \code{center} - equal padding on both edges.
\item \code{left} - signal at the start, padding appended after ("pad after").
\item \code{right} - padding prepended before, signal at the end ("pad before").
\item \code{feature} - each window is shifted so that \code{align_feature} lands on a
shared anchor index, padding the remainder. This is the alignment used to
stack beats on the R wave.
}

For \code{feature} alignment with no explicit \code{target_samples}, the output length
is derived so that no window is truncated: it spans the largest pre-feature
extent plus the largest post-feature extent seen across the collection, and
the feature is anchored at that common pre-feature offset. When
\code{target_samples} is supplied, the feature is anchored at \code{feature_position}
(default: the window centre) and any samples that fall outside the target
are truncated.
}
\section{What the fill claims}{


Every padded sample is a claim about signal that was never recorded, and the
three fills make different ones.

\code{NA} claims nothing, which is why it is the default. The cost is that a
padded collection cannot go straight into a matrix method, and the obvious
handling is the wrong one: where the padded length exceeds the median wave
span - as it must, or nothing would be padded - \emph{every} window carries some
\code{NA}, so dropping incomplete cases empties the matrix rather than trimming
it.

\code{"edge"} claims the signal held its last observed value. For a window whose
pad region is isoelectric - the TP and PR segments either side of a P wave,
say - that is close to true and is the fill to reach for. Across a wave it
is not, and it flattens the tails it invents.

\code{0} claims the potential was zero there. Because the windows are not
baseline-corrected (see the return value), zero is not even the isoelectric
line; it is wherever the recording's DC offset happens to put it.
}

\section{Guiding channel}{


A delineating annotator may be run over a record once, or once per lead. Run
per lead it writes twelve independent copies of every fiducial, separated
only by the \code{channel} column, and such a table does not describe beats until
it is resolved to a single lead: pooled, it reports twelve times as many
beats as the record contains, and taking the first match at each point
silently follows whichever lead happens to sort first.

So every function that reads fiducials - \code{\link[=get_windows]{get_windows()}},
\code{\link[=learn_template]{learn_template()}}, \code{\link[=median_window]{median_window()}} and the other window transforms,
\code{\link[=extract_f_waves]{extract_f_waves()}}, \code{\link[=vectorcardiogram]{vectorcardiogram()}} - takes a \code{channel}, and resolves
it the same way:
\itemize{
\item annotations spanning more than one channel with no \code{channel} given are an
error, not a guess;
\item a \code{channel} that the annotations do not carry is an error, which is what
catches a numbering convention that does not match;
\item the global channel \code{0} is not a lead, so it never counts toward the span
and is always kept alongside the chosen lead.
}

The channel may be a number or a channel name (\code{"II"}), resolved against the
record's own header. A number is matched literally against the \code{channel}
column, and the numbering there belongs to the annotator that wrote the
file. Read it off the record rather than assuming:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{table(get_annotation(x)$channel)
}\if{html}{\out{</div>}}

Only the boundaries are restricted to one lead. Windows still carry the
signal for every channel, which is what makes a windowed beat usable as a
vectorcardiogram.
}

\seealso{
\code{\link[=baseline_window]{baseline_window()}}, \code{\link[=median_window]{median_window()}}, \code{\link[=normalize_window]{normalize_window()}},
\code{\link[=by_beat]{by_beat()}} for cutting windows equal in the first place
}
