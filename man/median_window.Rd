% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/windows-transform.R
\name{median_window}
\alias{median_window}
\title{Collapse windows to a single median beat}
\usage{
median_window(
  x,
  align_feature = NULL,
  channel = NULL,
  na.rm = TRUE,
  channel_criteria = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{windows} object (or list of \code{EGM} objects) to reduce.}

\item{align_feature}{Optional feature used to pad-align windows of unequal
length before averaging, given as a \code{character} type symbol (e.g. "N", the
QRS peak) or a named list of annotation criteria. If \code{NULL} (default), the
windows must already share a common length.}

\item{channel}{The lead whose \code{align_feature} anchors the alignment, given as
a channel number or name. Required when the annotations span more than one
channel; see the channels section.}

\item{na.rm}{Logical passed to \code{\link[stats:median]{stats::median()}}; if TRUE (default), missing
values are ignored when averaging.}

\item{channel_criteria}{Superseded name for \code{channel}, still accepted.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A single \code{EGM} object representing the median beat.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Reduces a \code{windows} collection to a single representative \code{EGM} by taking the
element-wise median across all windows, lead by lead. Given an ECG windowed
into its individual beats, this returns the "median beat" - a robust template
that suppresses beat-to-beat noise and ectopy while preserving the dominant
morphology.
}
\details{
The median is computed per sample and per lead, so every window must
share the same length and the same set of leads. Beats windowed from raw
rhythm are generally \emph{not} the same length (see \code{\link[=get_windows]{get_windows()}}), so this
function offers to align them first: when \code{align_feature} is supplied and
the windows differ in length, they are padded and anchored on that feature
via \code{\link[=pad_window]{pad_window()}} before the median is taken. If the windows differ in
length and no \code{align_feature} is given, an error is raised pointing to
\code{\link[=pad_window]{pad_window()}} or \code{\link[=normalize_window]{normalize_window()}} so the caller chooses the alignment
explicitly.

The returned beat carries the fiducials that produced it, each placed at the
median of its own positions across the aligned windows. Annotations are
matched between windows by channel, type, and how many of that pair came
before them in their own window, so the first QRS onset of one beat lines up
with the first of every other. A fiducial that most windows do not carry is
dropped, since it is not part of what the median describes. The header is
named \verb{<source>_median} and carries an info string recording how many
windows it came from.
}
\section{Guiding channel}{


A delineating annotator may be run over a record once, or once per lead. Run
per lead it writes twelve independent copies of every fiducial, separated
only by the \code{channel} column, and such a table does not describe beats until
it is resolved to a single lead: pooled, it reports twelve times as many
beats as the record contains, and taking the first match at each point
silently follows whichever lead happens to sort first.

So every function that reads fiducials - \code{\link[=get_windows]{get_windows()}},
\code{\link[=learn_template]{learn_template()}}, \code{\link[=median_window]{median_window()}} and the other window transforms,
\code{\link[=extract_f_waves]{extract_f_waves()}}, \code{\link[=vectorcardiogram]{vectorcardiogram()}} - takes a \code{channel}, and resolves
it the same way:
\itemize{
\item annotations spanning more than one channel with no \code{channel} given are an
error, not a guess;
\item a \code{channel} that the annotations do not carry is an error, which is what
catches a numbering convention that does not match;
\item the global channel \code{0} is not a lead, so it never counts toward the span
and is always kept alongside the chosen lead.
}

The channel may be a number or a channel name (\code{"II"}), resolved against the
record's own header. A number is matched literally against the \code{channel}
column, and the numbering there belongs to the annotator that wrote the
file - the WFDB tools number channels from \code{0}, others from \code{1}. Read it off
the record rather than assuming:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{table(get_annotation(x)$channel)
}\if{html}{\out{</div>}}

Only the boundaries are restricted to one lead. Windows still carry the
signal for every channel, which is what makes a windowed beat usable as a
vectorcardiogram.
}

