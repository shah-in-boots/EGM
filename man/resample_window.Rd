% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window.R
\name{resample_window}
\alias{resample_window}
\title{Resample windows to a common sampling frequency}
\usage{
resample_window(
  x,
  resample_frequency = NULL,
  interpolation_method = c("linear", "spline", "step"),
  preserve_class = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{windowed} object (or list of \code{EGM} objects) to resample.}

\item{resample_frequency}{Target sampling rate in Hz. \code{NULL} accepts the
current sampling rate and returns the windows unchanged, which lets callers
pass a possibly-\code{NULL} rate straight through.}

\item{interpolation_method}{The interpolation method used when resampling.
One of "linear" (default), "spline", or "step".}

\item{preserve_class}{Logical. If TRUE (default), returns a \code{windowed} object;
if FALSE, returns a plain list of \code{EGM} objects.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{windowed} object (or list) of resampled \code{EGM} objects.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Resamples every window in a \code{windowed} object to a shared sampling frequency,
up- or down-sampling as needed while preserving each window's duration. This
harmonises records captured at different rates (e.g. mixing 250 Hz and 500 Hz
ECGs) so that downstream operations such as \code{\link[=median_window]{median_window()}} or
\code{\link[=standardize_windows]{standardize_windows()}} compare like with like.
}
\details{
Resampling is duration-preserving: a 0.8 s beat stays 0.8 s long but
its sample count scales with the frequency ratio. Signal values are
re-interpolated onto the new grid, the per-window header frequency and
sample count are updated, and annotation sample indices are rescaled to the
new grid. The same machinery is available inline via the
\code{resample_frequency} argument of \code{\link[=window]{window()}}.
}
