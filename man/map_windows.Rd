% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/windows-class.R
\name{map_windows}
\alias{map_windows}
\title{Apply a function across a collection of windows}
\usage{
map_windows(x, f, ...)
}
\arguments{
\item{x}{A \code{windows} object or list of \code{EGM} objects.}

\item{f}{A function applied to each window.}

\item{...}{Additional arguments passed to \code{f}.}
}
\value{
A \code{windows} object when all results are \code{EGM} objects, otherwise a
list.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Applies \code{f} to each \code{EGM} in a collection. When every result is itself an
\code{EGM} the collection is rebuilt as a \code{windows} object with its provenance
carried forward; otherwise a plain list of results is returned, which is the
natural shape for extracting per-window measurements.
}
\examples{
\dontrun{
beats <- get_windows(read_wfdb("ecg", test_path(), "ecgpuwave"))

# Per-window measurement -> plain list
map_windows(beats, function(w) nrow(w$signal))

# EGM-valued transform -> windows object, provenance preserved
map_windows(beats, function(w) w)
}

}
