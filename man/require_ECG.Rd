% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecg.R
\name{require_ECG}
\alias{require_ECG}
\title{Require a record to be a surface 12-lead ECG}
\usage{
require_ECG(object, leads = NULL, what = "This analysis")
}
\arguments{
\item{object}{An \code{EGM} or \code{ECG} object.}

\item{leads}{Optional \code{character} vector of canonical lead names that must be
present. If \code{NULL} (default), any non-empty surface lead set is accepted.}

\item{what}{A \code{character} naming the analysis, used in error messages.}
}
\value{
An \code{ECG} object holding only surface leads.
}
\description{
The gate for analyses defined only on the body-surface potential.
It coerces an \code{EGM} to an \link{ECG} with \code{\link[=as_ECG]{as_ECG()}}, then checks that the leads
the analysis needs are present. Every ECG-only function opens with this call,
so the contract lives in one place.
}
\details{
Two levels of strictness are supported. With \code{leads = NULL} any
non-empty surface lead set is accepted, which suits analyses that degrade
gracefully - fibrillatory wave extraction reads each lead more or less
independently and works, less precisely, on fewer of them. Naming \code{leads}
makes them mandatory, which suits analyses that are undefined without a
specific set - the Kors transform is a fixed linear combination of eight
leads and cannot be evaluated when one is missing.

Reporting belongs to \code{\link[=as_ECG]{as_ECG()}}, which says what it discarded, and to the
\code{ECG} constructor, which warns on an incomplete lead set. This function only
enforces.
}
\keyword{internal}
