% Generated by hand because roxygen2 was not available in the execution environment.
\name{wfdb_annotation_labels}
\alias{wfdb_annotation_labels}
\alias{wfdb_annotation_decode}
\title{Standard WFDB annotation nomenclature}
\description{
Provides the standard label definitions used by WFDB annotation files. The helper
functions make it easier to interpret the contents of an \link{annotation_table}
by exposing the symbol, mnemonic, and description that correspond to the WFDB
label store values defined in the WFDB Applications Guide.
}
\details{
The reference table is derived from the WFDB Applications Guide and matches the
canonical label store values used by the WFDB software distribution. Entries
that are not currently defined by the specification are omitted.
}
\usage{
wfdb_annotation_labels(symbol = NULL, label_store = NULL)

wfdb_annotation_decode(annotation, column = "type")
}
\arguments{
\item{symbol}{Optional character vector of WFDB annotation symbols used to filter the
results.}

\item{label_store}{Optional integer vector of label store values used to filter the results.}

\item{annotation}{An \link{annotation_table} or compatible data frame whose annotation
symbols or label store values should be augmented with the standard WFDB
nomenclature.}

\item{column}{Name of the column within \code{annotation} that contains either the WFDB
symbol (the default is the \code{type} column) or the label store values. If the
column is numeric it is matched on \code{label_store}, otherwise a symbol lookup
is performed.}
}
\value{
\code{wfdb_annotation_labels()} returns a data frame with columns \code{label_store},
\code{symbol}, \code{mnemonic}, \code{description}, and \code{is_qrs}.
\code{wfdb_annotation_decode()} returns the input annotation table with the WFDB
nomenclature columns appended.
}
\references{
Moody GB. \emph{WFDB Applications Guide}. PhysioNet.
\url{https://www.physionet.org/physiotools/wag/}.
}
\examples{
wfdb_annotation_labels()

wfdb_annotation_labels(symbol = c("N", "V"))

ann <- annotation_table(
  annotator = "example",
  sample = c(100L, 200L),
  type = c("N", "V")
)

wfdb_annotation_decode(ann)
}
